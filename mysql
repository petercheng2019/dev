# B+树
1. 叶子节点存储数据，节点之间有指针相连
key为bigint的时候，是8个字节。指针在mysql是6个字节。一共14个字节，一页可以占用1170个索引指针。2层索引，可以存放1170*1170个指针，3层数据，每页16条，总共可放两千万条数据。

适合文件索引和数据库索引。
1. B+tree的磁盘读写代价更低
2. 查询效率稳定
3. 范围查询效率高

缺点：产生大量的随机IO，逻辑上连续的叶子节点在物理上往往不连续，甚至分离的很远。大量的随机写，比如更新的数据id跨度很大